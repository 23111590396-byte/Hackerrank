# PRIMARY — sign up free at console.groq.com (email only, no credit card)
GROQ_API_KEY=your_groq_api_key_here

# FALLBACK — sign up free at aistudio.google.com (Google account only)
GEMINI_API_KEY=your_gemini_api_key_here

# Config (optional — defaults are set in code)
PRIMARY_MODEL=llama-3.3-70b-versatile
FALLBACK_MODEL=gemini-2.0-flash
MAX_TOKENS=800
TOP_K_CHUNKS=3
TEMPERATURE=0.1
